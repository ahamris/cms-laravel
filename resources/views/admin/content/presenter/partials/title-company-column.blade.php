<div>
    <div class="text-sm text-zinc-900 dark:text-white">{{ $item->title ?: 'No title' }}</div>
    <div class="text-sm text-zinc-500 dark:text-zinc-400">{{ $item->company ?: 'No company' }}</div>
</div>
